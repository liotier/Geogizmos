{
  "name": "geogizmos",
  "private": true,
  "type": "module",
  "description": "Tooling for the Geogizmos static web apps (Inundator, Isosmfar) - no runtime dependencies, this package.json exists only for tests and linting.",
  "scripts": {
    "test": "npm run test:inundator && npm run test:isosmfar",
    "test:inundator": "cd Inundator && node --test",
    "test:isosmfar": "cd Isosmfar && node --test",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@turf/turf": "^7.3.5",
    "eslint": "^9.19.0"
  }
}
